% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coil.R
\name{download_coil20}
\alias{download_coil20}
\title{Download COIL-20}
\format{
If \code{as = "data.frame"}, the default, a data frame with 16,385
variables:
\itemize{
\item \code{x1_y1}, \code{x1_y2}, ... \code{x1_y128}, \code{x2_y1}, ... \code{x128_y128}: Pixel values
ranging from 0 to 1. The \code{x} index increases from left to right, and the
\code{y} index increases from top to bottom, so \code{x1_y1} is top left and
\code{x128_y128} is bottom right.
\item \code{Label}: The object id, stored as a factor with levels 1 to 20.
}

Row names are \code{"<object>_<pose>"}, where \verb{<object>} is the object id and
\verb{<pose>} is the pose id, from 0 to 71.
}
\usage{
download_coil20(
  file = NULL,
  cleanup = TRUE,
  verbose = FALSE,
  as = c("data.frame", "matrix")
)
}
\arguments{
\item{file}{File path to download the ZIP archive to. If \code{NULL}, a file in a
temporary work directory is used.}

\item{cleanup}{If \code{TRUE}, delete temporary download and extraction files
before returning. If \code{file} is supplied, only the downloaded ZIP and the
dedicated extraction directory are removed.}

\item{verbose}{If \code{TRUE}, log download and extraction progress.}

\item{as}{Return format. Use \code{"data.frame"} for the original wide data frame
shape, or \code{"matrix"} for a list with \code{data}, \code{labels}, \code{poses}, and \code{ids}.}
}
\value{
If \code{as = "data.frame"}, a data frame containing the COIL-20 dataset.
If \code{as = "matrix"}, a list containing a numeric matrix with one image per
row, factor labels, integer poses, and row ids.
}
\description{
Download the processed Columbia Object Image Library COIL-20 dataset.
}
\details{
Downloads and expands the processed COIL-20 ZIP archive and converts the PNG
files to a data frame or matrix-list result. COIL-20 contains 20 objects,
each represented by 72 grayscale poses. Each image has resolution 128 x 128.

The optional \href{https://cran.r-project.org/package=png}{png} package is
required when reading the downloaded PNG files.
}
\examples{
\dontrun{
coil20 <- download_coil20(verbose = TRUE)
show_coil_object(coil20, object = 5, pose = 4)
}
}
\references{
The Columbia Object Image Library COIL-20
\url{https://cave.cs.columbia.edu/repository/COIL-20}
}
